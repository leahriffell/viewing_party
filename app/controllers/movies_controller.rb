class MoviesController < ApplicationController
  def index; end

  # same route but different views | same route but different content 
  # pass needed API params with button or search bar. then if params include top 40, render top view. else, return search result view. 
end
