<% @movies.each do |movie| %>
  <div class='movie'>
    <%= link_to "#{movie.title}", movie_path(movie.id), class: 'title' %><br>
    <span>Vote average:</span>
    <span class='vote_avg'><%= movie.vote_average %></span><br><br>
  </div>
<% end %>